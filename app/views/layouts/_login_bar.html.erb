<% if current_user %>
    <% # current_user will return true if a user is logged in %>
    <%= "Logged in as #{current_user.email}" %> | <%= link_to 'Log Out', logout_path, method: :delete %>
  <% else %>
    <% # not logged in %>
    <%= link_to 'Log In', login_path %> or <%= link_to 'Sign Up', new_user_path %>
  <% end %>
  <hr>